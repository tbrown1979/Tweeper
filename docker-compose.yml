nginx:
  build: nginx/
  links:
    - website:website
  ports:
    - "80:80"
website:
  build: .
  command: java -Xmx64m -jar /opt/app/app.jar
  links:
    - elasticsearch
  ports:
    - "8080:8080"
elasticsearch:
  image: elasticsearch
